{
	"name": "nats-tower-integration-tests",
	"version": "1.0.0",
	"private": true,
	"description": "End-to-end integration tests for NATS Tower (Playwright + Docker + nats-server + nats CLI)",
	"type": "module",
	"scripts": {
		"test": "playwright test",
		"test:headed": "playwright test --headed",
		"test:ui": "playwright test --ui",
		"install:browsers": "playwright install chromium"
	},
	"devDependencies": {
		"@playwright/test": "1.49.1",
		"@types/node": "22.10.2"
	}
}
